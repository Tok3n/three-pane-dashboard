// Generated by CoffeeScript 1.7.1
(function() {
  (function($) {
    var changeClassTransitional;
    changeClassTransitional = function(el, cl, kind, options) {
      var dfd, duration;
      options = options || {};
      el[kind + "Class"](cl);
      duration = 1000 * parseFloat(el.css("transition-duration").slice(0, -1), 10);
      dfd = $.Deferred();
      if (duration) {
        el.on("transitionend", function(event) {
          clearInterval(interval);
          dfd.resolveWith(el, [event]);
          return el.off("transitionend");
        });
      } else {
        dfd.resolveWith(el, [false]);
      }
      return dfd.promise();
    };
    return $.fn.extend({
      addClassTransitional: function(cl, options) {
        return changeClassTransitional(this, cl, "add");
      },
      removeClassTransitional: function(cl, options) {
        return changeClassTransitional(this, cl, "remove");
      },
      toggleClassTransitional: function(cl, options) {
        return changeClassTransitional(this, cl, "toggle");
      }
    });
  })(jQuery);

}).call(this);
